# Methods added to this helper will be available to all templates in the application.
module ApplicationHelper
# menu
  def menu_display
    # Security Logic to determine which menu option to dispay
   #  if session[:user].nil?
   #   else
  

    #  if session[:user].usur_rol == "3"
    #    else  
    #       ret = "<ul>"
    #    ret += "<li>"
    #     ret += link_to 'Articulos',         :action => 'index', :controller => "articulos"
    #     ret += "</li>"
    #     ret += "<li>"
    #     ret += link_to 'Lista de Precios', :action => 'index', :controller => "listaprecios"
    #     ret += "</li>"
    #   ret += "</ul>"
           # elsif session[:user].usur_rol == "3"
            #   link_to '[Client Manager]', :action => 'index',   :controller => "reclamo"
             #   link_to '[User Manager]',   :action => 'login',   :controller => "usuario"
            #   link_to '[Logout]',         :action => 'logout', :controller => "usuario"
    
  # end
   

#end
end


end
